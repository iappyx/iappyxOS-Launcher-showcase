{
  "page": {
    "translationX":   "-p * w * max(0, -sign(p))",
    "rotationX":      "-10 * abs(p) * max(0, -sign(p))",
    "translationY":   "-h * 0.04 * abs(p) * max(0, -sign(p))",
    "translationZ":   "8 * density * abs(p) * max(0, -sign(p))",
    "scaleX":         "1 - 0.08 * abs(p) * max(0, -sign(p))",
    "scaleY":         "1 - 0.08 * abs(p) * max(0, -sign(p))",
    "alpha":          "1 - abs(p) * 0.6 * max(0, -sign(p))",
    "cameraDistance": "12000 * density"
  }
}
